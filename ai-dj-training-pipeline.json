{
  "components": {
    "comp-evaluate-model": {
      "executorLabel": "exec-evaluate-model",
      "inputDefinitions": {
        "artifacts": {
          "model": {
            "artifactType": {
              "schemaTitle": "system.Model",
              "schemaVersion": "0.0.1"
            }
          },
          "preprocessed_dataset": {
            "artifactType": {
              "schemaTitle": "system.Dataset",
              "schemaVersion": "0.0.1"
            }
          }
        }
      },
      "outputDefinitions": {
        "artifacts": {
          "metrics": {
            "artifactType": {
              "schemaTitle": "system.Metrics",
              "schemaVersion": "0.0.1"
            }
          }
        }
      }
    },
    "comp-import-bigquery": {
      "executorLabel": "exec-import-bigquery",
      "inputDefinitions": {
        "parameters": {
          "project_id": {
            "parameterType": "STRING"
          }
        }
      },
      "outputDefinitions": {
        "artifacts": {
          "output_dataset": {
            "artifactType": {
              "schemaTitle": "system.Dataset",
              "schemaVersion": "0.0.1"
            }
          }
        }
      }
    },
    "comp-train-model": {
      "executorLabel": "exec-train-model",
      "inputDefinitions": {
        "artifacts": {
          "preprocessed_dataset": {
            "artifactType": {
              "schemaTitle": "system.Dataset",
              "schemaVersion": "0.0.1"
            }
          }
        },
        "parameters": {
          "n_estimators": {
            "defaultValue": 100.0,
            "isOptional": true,
            "parameterType": "NUMBER_INTEGER"
          }
        }
      },
      "outputDefinitions": {
        "artifacts": {
          "metrics": {
            "artifactType": {
              "schemaTitle": "system.Metrics",
              "schemaVersion": "0.0.1"
            }
          },
          "model": {
            "artifactType": {
              "schemaTitle": "system.Model",
              "schemaVersion": "0.0.1"
            }
          }
        }
      }
    },
    "comp-training-preprocess": {
      "executorLabel": "exec-training-preprocess",
      "inputDefinitions": {
        "artifacts": {
          "input_dataset": {
            "artifactType": {
              "schemaTitle": "system.Dataset",
              "schemaVersion": "0.0.1"
            }
          }
        }
      },
      "outputDefinitions": {
        "artifacts": {
          "output_dataset": {
            "artifactType": {
              "schemaTitle": "system.Dataset",
              "schemaVersion": "0.0.1"
            }
          }
        }
      }
    }
  },
  "deploymentSpec": {
    "executors": {
      "exec-evaluate-model": {
        "container": {
          "args": [
            "--executor_input",
            "{{$}}",
            "--function_to_execute",
            "evaluate_model"
          ],
          "command": [
            "sh",
            "-c",
            "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip || python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1 python3 -m pip install --quiet --no-warn-script-location 'pandas' 'scikit-learn' 'pyarrow' 'joblib'  &&  python3 -m pip install --quiet --no-warn-script-location 'kfp==2.16.0' '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"$0\" \"$@\"\n",
            "sh",
            "-ec",
            "program_path=$(mktemp -d)\n\nprintf \"%s\" \"$0\" > \"$program_path/ephemeral_component.py\"\n_KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         \"$program_path/ephemeral_component.py\"                         \"$@\"\n",
            "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import *\n\ndef evaluate_model(\n    model: Input[Model],\n    preprocessed_dataset: Input[Dataset],\n    metrics: Output[Metrics], # This makes results visible in Vertex UI\n):\n    import pandas as pd\n    import joblib\n    from sklearn.metrics import accuracy_score, f1_score, precision_score, recall_score\n\n    # 1. Load the data (Fixing the pathing bug)\n    df = pd.read_parquet(preprocessed_dataset.path)\n    X = df.drop(columns=[\"hit\"])\n    y = df[\"hit\"]\n\n    # 2. Load the EXACT model produced by the training component\n    # Note: Using the .joblib suffix we used in the training save\n    model_instance = joblib.load(model.path + \".joblib\")\n\n    # 3. Predict on the dataset\n    # TA NOTE: Ideally, this dataset should be a 'test' split \n    # that the training component NEVER saw.\n    y_pred = model_instance.predict(X)\n\n    # 4. Calculate Metrics\n    acc = float(accuracy_score(y, y_pred))\n    prec = float(precision_score(y, y_pred, zero_division=0))\n    rec = float(recall_score(y, y_pred))\n    f1 = float(f1_score(y, y_pred))\n\n    # 5. Log to Vertex AI Metadata (The MLOps Way)\n    metrics.log_metric(\"accuracy\", acc)\n    metrics.log_metric(\"precision\", prec)\n    metrics.log_metric(\"recall\", rec)\n    metrics.log_metric(\"f1_score\", f1)\n\n    print(f\"Evaluation complete. Accuracy: {acc}\")\n\n"
          ],
          "image": "python:3.10"
        }
      },
      "exec-import-bigquery": {
        "container": {
          "args": [
            "--executor_input",
            "{{$}}",
            "--function_to_execute",
            "import_bigquery"
          ],
          "command": [
            "sh",
            "-c",
            "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip || python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1 python3 -m pip install --quiet --no-warn-script-location 'pandas' 'pyarrow' 'google-cloud-bigquery' 'db-dtypes'  &&  python3 -m pip install --quiet --no-warn-script-location 'kfp==2.16.0' '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"$0\" \"$@\"\n",
            "sh",
            "-ec",
            "program_path=$(mktemp -d)\n\nprintf \"%s\" \"$0\" > \"$program_path/ephemeral_component.py\"\n_KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         \"$program_path/ephemeral_component.py\"                         \"$@\"\n",
            "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import *\n\ndef import_bigquery(\n    output_dataset: Output[Dataset],\n    project_id: str,\n):\n    from google.cloud import bigquery\n    import pandas as pd\n\n    # 1. Initialize client with explicit project\n    client = bigquery.Client(project=project_id)\n\n    query = \"\"\"\n    SELECT *\n    FROM `ai-dj-487610.Spotify_Tracks.tracks`\n    \"\"\"\n\n    # 2. Add progress logging to see where it hangs in the Vertex logs\n    print(\"Starting BigQuery Query...\")\n\n    # Use to_dataframe() - ensure db-dtypes is present\n    df = client.query(query).to_dataframe()\n\n    print(f\"Query successful. Retrieved {len(df)} rows.\")\n\n    # 3. Explicitly use pyarrow engine and ensure path is handled\n    # Vertex AI paths don't always have the .parquet extension, \n    # so we force the engine to avoid Pandas confusion.\n    df.to_parquet(output_dataset.path, engine='pyarrow', index=False) \n\n    # 4. Metadata for the Vertex UI\n    output_dataset.metadata['row_count'] = len(df)\n    output_dataset.metadata['source'] = 'BigQuery'\n\n    print(f\"Dataset saved to {output_dataset.path}\")\n\n"
          ],
          "image": "python:3.10"
        }
      },
      "exec-train-model": {
        "container": {
          "args": [
            "--executor_input",
            "{{$}}",
            "--function_to_execute",
            "train_model"
          ],
          "command": [
            "sh",
            "-c",
            "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip || python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1 python3 -m pip install --quiet --no-warn-script-location 'pandas' 'scikit-learn' 'pyarrow' 'joblib'  &&  python3 -m pip install --quiet --no-warn-script-location 'kfp==2.16.0' '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"$0\" \"$@\"\n",
            "sh",
            "-ec",
            "program_path=$(mktemp -d)\n\nprintf \"%s\" \"$0\" > \"$program_path/ephemeral_component.py\"\n_KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         \"$program_path/ephemeral_component.py\"                         \"$@\"\n",
            "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import *\n\ndef train_model(\n    preprocessed_dataset: Input[Dataset],\n    model: Output[Model],\n    metrics: Output[Metrics], # Adding Metrics for the Vertex UI\n    n_estimators: int = 100,  # Parameterizing for better MLOps\n):\n    import pandas as pd\n    import joblib\n    from sklearn.ensemble import RandomForestClassifier\n    from sklearn.metrics import accuracy_score\n\n    # 1. Load data using the direct path (no os.path.join)\n    df = pd.read_parquet(preprocessed_dataset.path)\n\n    # 2. Split features and target\n    X = df.drop(columns=[\"hit\"])\n    y = df[\"hit\"]\n\n    # 3. Train model\n    # Note: We use the parameter passed from the pipeline config\n    model_instance = RandomForestClassifier(\n        n_estimators=n_estimators,\n        max_depth=None,\n        random_state=42,\n        class_weight=\"balanced\"\n    )\n\n    model_instance.fit(X, y)\n\n    # 4. Evaluation (Basic internal check)\n    y_pred = model_instance.predict(X)\n    acc = accuracy_score(y, y_pred)\n\n    # Log to Vertex AI Metrics artifact\n    metrics.log_metric(\"accuracy\", float(acc))\n    metrics.log_metric(\"n_estimators\", n_estimators)\n\n    # 5. Feature importance\n    importance = model_instance.feature_importances_\n    feature_imp = pd.DataFrame({\"feature\": X.columns, \"importance\": importance})\n    top_5 = feature_imp.sort_values(by=\"importance\", ascending=False)[\"feature\"].head(5).tolist()\n\n    # 6. Save outputs\n    # model.path is a generated URI by Vertex. Save the file directly there.\n    # We save as 'model.joblib' - Vertex AI Prediction expects this naming convention.\n    suffix = \".joblib\"\n    joblib.dump(model_instance, model.path + suffix)\n\n    # Save metadata to the Model artifact (Great for traceability!)\n    model.metadata[\"framework\"] = \"scikit-learn\"\n    model.metadata[\"top_5_features\"] = top_5\n    model.metadata[\"columns\"] = X.columns.tolist()\n\n    print(f\"Model saved with accuracy: {acc}\")\n\n"
          ],
          "image": "python:3.10"
        }
      },
      "exec-training-preprocess": {
        "container": {
          "args": [
            "--executor_input",
            "{{$}}",
            "--function_to_execute",
            "training_preprocess"
          ],
          "command": [
            "sh",
            "-c",
            "\nif ! [ -x \"$(command -v pip)\" ]; then\n    python3 -m ensurepip || python3 -m ensurepip --user || apt-get install python3-pip\nfi\n\nPIP_DISABLE_PIP_VERSION_CHECK=1 python3 -m pip install --quiet --no-warn-script-location 'pandas' 'pyarrow'  &&  python3 -m pip install --quiet --no-warn-script-location 'kfp==2.16.0' '--no-deps' 'typing-extensions>=3.7.4,<5; python_version<\"3.9\"' && \"$0\" \"$@\"\n",
            "sh",
            "-ec",
            "program_path=$(mktemp -d)\n\nprintf \"%s\" \"$0\" > \"$program_path/ephemeral_component.py\"\n_KFP_RUNTIME=true python3 -m kfp.dsl.executor_main                         --component_module_path                         \"$program_path/ephemeral_component.py\"                         \"$@\"\n",
            "\nimport kfp\nfrom kfp import dsl\nfrom kfp.dsl import *\nfrom typing import *\n\ndef training_preprocess(\n    input_dataset: Input[Dataset],\n    output_dataset: Output[Dataset],\n):\n    import pandas as pd\n\n    # Vertex AI handles the pathing. \n    # input_dataset.path is the exact location of the parquet file from the previous step.\n    df = pd.read_parquet(input_dataset.path)\n\n    # --- Your preprocessing logic ---\n    # Creating the target variable for the AI DJ\n    df[\"hit\"] = (df[\"popularity\"] >= 65).astype(int)\n\n    # Dropping non-feature columns\n    df = df.drop(columns=[\"popularity\", \"Unnamed: 0\"], errors=\"ignore\")\n\n    # Feature Selection: Only numeric features for the models (RF/GB/KNN)\n    X = df.drop(\"hit\", axis=1)\n    X = X.select_dtypes(include=[\"number\"])\n    y = df[\"hit\"]\n\n    # Combine into one dataframe for the training component\n    processed_df = X.copy()\n    processed_df[\"hit\"] = y\n\n    # Save output: Again, output_dataset.path is the final file destination.\n    # No need for os.makedirs or joining \"data.parquet\"\n    processed_df.to_parquet(output_dataset.path)\n\n    print(f\"Preprocessed dataset saved with {len(processed_df)} rows and {len(X.columns)} features.\")\n\n"
          ],
          "image": "python:3.10"
        }
      }
    }
  },
  "pipelineInfo": {
    "name": "ai-dj-training-pipeline"
  },
  "root": {
    "dag": {
      "tasks": {
        "evaluate-model": {
          "cachingOptions": {
            "enableCache": true
          },
          "componentRef": {
            "name": "comp-evaluate-model"
          },
          "dependentTasks": [
            "train-model",
            "training-preprocess"
          ],
          "inputs": {
            "artifacts": {
              "model": {
                "taskOutputArtifact": {
                  "outputArtifactKey": "model",
                  "producerTask": "train-model"
                }
              },
              "preprocessed_dataset": {
                "taskOutputArtifact": {
                  "outputArtifactKey": "output_dataset",
                  "producerTask": "training-preprocess"
                }
              }
            }
          },
          "taskInfo": {
            "name": "evaluate-model"
          }
        },
        "import-bigquery": {
          "cachingOptions": {
            "enableCache": true
          },
          "componentRef": {
            "name": "comp-import-bigquery"
          },
          "inputs": {
            "parameters": {
              "project_id": {
                "componentInputParameter": "project_id"
              }
            }
          },
          "taskInfo": {
            "name": "import-bigquery"
          }
        },
        "train-model": {
          "cachingOptions": {
            "enableCache": true
          },
          "componentRef": {
            "name": "comp-train-model"
          },
          "dependentTasks": [
            "training-preprocess"
          ],
          "inputs": {
            "artifacts": {
              "preprocessed_dataset": {
                "taskOutputArtifact": {
                  "outputArtifactKey": "output_dataset",
                  "producerTask": "training-preprocess"
                }
              }
            }
          },
          "taskInfo": {
            "name": "train-model"
          }
        },
        "training-preprocess": {
          "cachingOptions": {
            "enableCache": true
          },
          "componentRef": {
            "name": "comp-training-preprocess"
          },
          "dependentTasks": [
            "import-bigquery"
          ],
          "inputs": {
            "artifacts": {
              "input_dataset": {
                "taskOutputArtifact": {
                  "outputArtifactKey": "output_dataset",
                  "producerTask": "import-bigquery"
                }
              }
            }
          },
          "taskInfo": {
            "name": "training-preprocess"
          }
        }
      }
    },
    "inputDefinitions": {
      "parameters": {
        "model_version": {
          "defaultValue": "v1",
          "isOptional": true,
          "parameterType": "STRING"
        },
        "project_id": {
          "defaultValue": "ai-dj-487610",
          "isOptional": true,
          "parameterType": "STRING"
        }
      }
    }
  },
  "schemaVersion": "2.1.0",
  "sdkVersion": "kfp-2.16.0"
}